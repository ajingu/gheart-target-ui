# gheart-target-ui

A minimal demo project used as a target repo for [gheart](https://github.com/ajingu/gheart) (Tinder-style swiping for PR reviews).

## Usage

Open `index.html` directly in a browser, or serve the directory with any static file server.
