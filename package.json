{
  "name": "gheart-target-ui",
  "version": "0.1.0",
  "private": true,
  "description": "A minimal todo app target repo for gheart.",
  "scripts": {
    "dev": "node backend/server.js",
    "test": "node scripts/smoke-test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
